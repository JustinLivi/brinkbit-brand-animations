/* eslint-env mocha */
/* global expect */
/* eslint no-unused-expressions: 0 */

import alphabet from '../src/letters.js';
import A from '../src/letters.js';
import B from '../src/letters.js';
import C from '../src/letters.js';
import D from '../src/letters.js';
import E from '../src/letters.js';
import F from '../src/letters.js';
import G from '../src/letters.js';
import H from '../src/letters.js';
import I from '../src/letters.js';
import J from '../src/letters.js';
import K from '../src/letters.js';
import L from '../src/letters.js';
import M from '../src/letters.js';
import N from '../src/letters.js';
import O from '../src/letters.js';
import P from '../src/letters.js';
import Q from '../src/letters.js';
import R from '../src/letters.js';
import S from '../src/letters.js';
import T from '../src/letters.js';
import U from '../src/letters.js';
import V from '../src/letters.js';
import W from '../src/letters.js';
import X from '../src/letters.js';
import Y from '../src/letters.js';
import Z from '../src/letters.js';
import logo from '../src/letters.js';

describe( 'alphabet', () => {
    it( 'should be an object', () => {
        expect( alphabet ).to.be.an.object;
    });
});

describe( 'A', () => {
    it( 'should be an array', () => {
        expect( A ).to.be.an.array;
    });
});

describe( 'B', () => {
    it( 'should be an array', () => {
        expect( B ).to.be.an.array;
    });
});

describe( 'C', () => {
    it( 'should be an array', () => {
        expect( C ).to.be.an.array;
    });
});

describe( 'D', () => {
    it( 'should be an array', () => {
        expect( D ).to.be.an.array;
    });
});

describe( 'E', () => {
    it( 'should be an array', () => {
        expect( E ).to.be.an.array;
    });
});

describe( 'F', () => {
    it( 'should be an array', () => {
        expect( F ).to.be.an.array;
    });
});

describe( 'G', () => {
    it( 'should be an array', () => {
        expect( G ).to.be.an.array;
    });
});

describe( 'H', () => {
    it( 'should be an array', () => {
        expect( H ).to.be.an.array;
    });
});

describe( 'I', () => {
    it( 'should be an array', () => {
        expect( I ).to.be.an.array;
    });
});

describe( 'J', () => {
    it( 'should be an array', () => {
        expect( J ).to.be.an.array;
    });
});

describe( 'K', () => {
    it( 'should be an array', () => {
        expect( K ).to.be.an.array;
    });
});

describe( 'L', () => {
    it( 'should be an array', () => {
        expect( L ).to.be.an.array;
    });
});

describe( 'M', () => {
    it( 'should be an array', () => {
        expect( M ).to.be.an.array;
    });
});

describe( 'N', () => {
    it( 'should be an array', () => {
        expect( N ).to.be.an.array;
    });
});

describe( 'O', () => {
    it( 'should be an array', () => {
        expect( O ).to.be.an.array;
    });
});

describe( 'P', () => {
    it( 'should be an array', () => {
        expect( P ).to.be.an.array;
    });
});

describe( 'Q', () => {
    it( 'should be an array', () => {
        expect( Q ).to.be.an.array;
    });
});

describe( 'R', () => {
    it( 'should be an array', () => {
        expect( R ).to.be.an.array;
    });
});

describe( 'S', () => {
    it( 'should be an array', () => {
        expect( S ).to.be.an.array;
    });
});

describe( 'T', () => {
    it( 'should be an array', () => {
        expect( T ).to.be.an.array;
    });
});

describe( 'U', () => {
    it( 'should be an array', () => {
        expect( U ).to.be.an.array;
    });
});

describe( 'V', () => {
    it( 'should be an array', () => {
        expect( V ).to.be.an.array;
    });
});

describe( 'W', () => {
    it( 'should be an array', () => {
        expect( W ).to.be.an.array;
    });
});

describe( 'X', () => {
    it( 'should be an array', () => {
        expect( X ).to.be.an.array;
    });
});

describe( 'Y', () => {
    it( 'should be an array', () => {
        expect( Y ).to.be.an.array;
    });
});

describe( 'Z', () => {
    it( 'should be an array', () => {
        expect( Z ).to.be.an.array;
    });
});

describe( 'logo', () => {
    it( 'should be an array', () => {
        expect( logo ).to.be.an.array;
    });
});
